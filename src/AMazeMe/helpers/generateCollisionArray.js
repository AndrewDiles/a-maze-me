const partsPerCell = 5; // linear.  By area: 25
export default (layout) => {
	const result  = [];
	const numberOfRows = layout.length;
	const numberOfCols = layout[0].length;
	
	for (let row = 0; row < numberOfRows; row++) {
		const rowArray = [];
		for (let col = 0; col < numberOfCols; col++){
			for (let part = 0; part < partsPerCell; part++) {
				
			}
		}
	}

}